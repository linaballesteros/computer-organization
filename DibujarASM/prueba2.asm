// put bitmap location value in R12
	// put code return address in R13
	@SCREEN
	D=A
	@R12
	AD=D+M
	// row 7
	M=1
	// row 8
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 9
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 10
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 11
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 12
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 13
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 14
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 15
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 16
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 17
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 18
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 19
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 20
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 21
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 22
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 23
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 24
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 25
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 26
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 27
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 28
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 29
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 30
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 31
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// row 32
	D=A // D holds previous addr
	@32
	AD=D+A
	M=1
	// return
	@R13
	A=M